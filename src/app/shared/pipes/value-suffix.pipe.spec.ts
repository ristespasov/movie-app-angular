import { ValueSuffixPipe } from './value-suffix.pipe';

describe('ValueSuffixPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueSuffixPipe();
    expect(pipe).toBeTruthy();
  });
});
